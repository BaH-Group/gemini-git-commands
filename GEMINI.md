# Gemini Git & Python Commands Extension

This extension provides custom commands for Git workflows and Python project scaffolding.

## Git Commands
- `git:add`: Stage changes.
- `git:commit`: Generate commit messages from staged changes.
- `git:push`: Push changes to origin.

## Python Commands
- `python:init`: Scaffold a Python project with `python-dotenv` and a virtual environment.
